name: code_combiner
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  fluent_ui: ^4.13.0
  provider: ^6.1.5+1
  file_picker: ^10.3.10
  path: ^1.9.1
  intl: ^0.20.2
  system_theme: ^3.2.0
  
  # 🚀 Phase 1: The Glass Stack
  # FIXED: Downgraded to actual existing version because 3.0.0 is fake news
  bitsdojo_window: ^0.1.6 
  flutter_acrylic: ^1.0.0
  google_fonts: ^6.1.0
  desktop_drop: ^0.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true