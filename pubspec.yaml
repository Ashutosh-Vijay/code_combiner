name: code_combiner
description: "A high-performance, aesthetically pleasing utility designed to prepare massive codebases for Large Language Model (LLM) context windows. Built with Flutter, Mica/Glass UI, and pure rage against bad tooling."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  fluent_ui: ^4.13.0
  provider: ^6.1.5+1
  file_picker: ^10.3.10
  path: ^1.9.1
  intl: ^0.20.2
  system_theme: ^3.2.0
  
  # 🚀 Phase 1: The Glass Stack
  bitsdojo_window: ^0.1.6
  flutter_acrylic: ^1.0.0
  google_fonts: ^6.1.0
  desktop_drop: ^0.4.0
  
  # 🧠 Phase 2: Memory
  shared_preferences: ^2.2.2

  # 🔗 Phase 4: Identity
  url_launcher: ^6.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  # 🎨 Icon Gen
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true

# --- ICON CONFIGURATION ---
flutter_launcher_icons:
  android: false
  ios: false
  image_path: "assets/app.ico"
  windows:
    generate: true
    icon_size: 256